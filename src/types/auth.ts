
export interface LoginCredentials {
  email: string;
  passwordAttempt: string;
}

export interface RegisterCredentials {
  email: string;
  passwordAttempt: string;
}

// Keep this file simple for now, can be expanded if needed.
// The main AppUser type is in src/types/index.ts
